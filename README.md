# Lightwork AI — landing page

One-page site for the AI consultancy. `index.html` is the published page; it is
self-contained apart from two web fonts.

- `index.html` — the live page (currently identical to `landing-vc14.html`)
- `landing-vcNN.html` — version history, newest highest

## Deploy

GitHub Pages serves `index.html` from the default branch. Commit and push to publish.
